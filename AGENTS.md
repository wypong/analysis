# Local Instructions

- For analysis work in this repository, use the notes in `/home/pong/courses/401/lect/` as reference context.
- Relevant books are available in `/home/pong/courses/401/book`.
- Python libraries are installed in the virtual environment at `~/pt/`.
- In PreTeXt definitions, use `<term>...</term>` rather than `<em>...</em>`.
- Terminology: a set is `countably infinite` iff it has the same cardinality as `\N`.
- Terminology: a set is `countable` iff it is finite or countably infinite; avoid the phrase `at most countable`.
